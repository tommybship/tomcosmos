# Ephemeris kernels (large binary files, downloaded on demand)
data/kernels/
# skyfield drops its default-loader downloads in cwd; catch strays.
/*.bsp
/*.bpc
/*.tf
/*.tls
/*.tpc

# Simulation outputs
runs/

# Cached external-API responses (Horizons state vectors, etc.). Safe
# to delete; contents are deterministic and re-fetchable on demand.
data/cache/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env

# Test / type-check caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
